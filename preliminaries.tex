% !TeX root = courtade-kumar.tex
\section{Preliminaries}
\label{sec:preliminaries}
Let the random variables $U = U_1U_2$ and $V= V_1V_2$ take
values in the same finite set. The divergence of $U$ from
$V$ and the divergence of $U_2\emid U_1$ from $V_2\emid V_1$
are respectively
\begin{align*}
\kldiv{U}{V}
  &\defeq \E_{u\sim U}
     \log \frac{\Pr[U=u]}{\Pr[V=u]},\\
\kldiv{U_2\emid U_1}{V_2\emid V_1}
  &\defeq \E_{u_1\sim U_1}
     \kldiv{U_2\emid U_1=u_1}{V_2\emid V_1=u_1}.
\end{align*}
With this notation in hand, we can observe the
following chain rule holds
\begin{align*}
\kldiv{U}{V}
  &= \kldiv{U_1}{V_1} + \kldiv{U_2 \emid U_1}{V_2\emid V_1}.
\intertext{The mutual information of $U_1$ and $U_2$ is
then given by}
\muti{U_1}{U_2}
  &= \kldiv{U_2\emid U_1}{U_2} = \kldiv{U_1\emid U_2}{U_1}.
\end{align*}
Let $A$, $B$ be two bit valued random variables
with $\E[A]=a$ and $\E[B]=b$. We define the binary
divergence function
\begin{align*}
\kldivb{a}{b} &\defeq\kldiv{A}{B}\\
  &\phantom{:}= a\log\frac ab + (1-a)\log\frac{1-a}{1-b}.
\intertext{We use the following fact about binary divergence}
\end{align*}
