# Final adversarial review (18 Sept 2026)

After the full gate accepted `CKF2.exactTargetAccepted`, three independent reviewer agents were launched with
distinct adversarial briefs (each told to find a reason the result does NOT mean what it appears to mean), followed
by a completeness critic. Their structured verdicts are reproduced below verbatim (machine-read output; light
truncation only where the agent's own text was cut by the output schema). Verdict scale: SOUND / CONCERN / DEFECT.


## Lens 1 — Statement semantics (is `CKF2.Spec.Target` the Courtade–Kumar inequality?)

**Verdict: SOUND**

CKF2.Spec.Target is exactly the Courtade-Kumar inequality in natural-log units with no weakening: uniform input on the Boolean cube, independent BSC(p) channel (row sums = 1 for all real p, hence Y uniform and the posterior formula is the Bayes posterior), I(f(X);Y) = h(mean) - avg_y h(posterior) in nats, RHS = ln 2 - h(p), quantified over all n : N (including 0), all Boolean f, and all real p with 0 <= p <= (1/2 : R). Lean's Real.log junk values are never reached in range (posterior and mean lie in [0,1], proved) and Real.log 0 = 0 reproduces the manuscript's 0 ln 0 := 0 convention. Spot checks proved in a scratch file (compiles cleanly, no sorry): n = 1 coordinate gives retainedInformation = log 2 - entropy p exactly (bound attained), n = 2 parity posterior = 2p(1-p) or (1-p)^2 + p^2 by output parity. Trusted-input hashes match, Mathlib checkout is pristine at the manifest rev, matches_f1_target is rfl, axioms of CK.courtadeKumar_proved are [propext, Classical.choice, Quot.sound]. No gap between the Lean statement and the informal conjecture / manuscript Theorem thm:main.

### Findings

- Trusted inputs verified: sha256 of lean/CK/Definitions.lean, lean/CKF2/Spec.lean, CompletionGate.lean.template, DECLARATIONS.json, lean/lean-toolchain all match ~/ck_f2/TRUSTED_INPUT_HASHES.json; F2CompletionGate.lean is byte-identical to the template; Mathlib checkout at ~/ck_lean/.lake/packages/mathlib is clean (git status empty) at rev c44e0c8ee63ca166450922a373c7409c5d26b00b, matching lake-manifest.json. Both trusted .lean files contain no notation/macro/instance/set_option (other than autoImplicit false) that could alter meaning; `_root_.Real.log`, `_root_.Finset.univ.sum/.prod` rfl-tests in the scratch file confirm no name shadowing inside the CK / CKF2.Spec namespaces.

- (a) Channel/cube model is exactly uniform X on {T,F}^n (relabelling of {-1,1}^n) through independent BSC(p): channel p y x = prod_i (if y i = x i then 1-p else p) = P(Y=y | X=x). Bayes justification PROVED in scratch: `channel_row_sum : sum_x channel p y x = 1` for EVERY real p (via Fintype.prod_sum), hence `output_marginal_uniform : average (fun x => channel p y x) = 1/2^n`, i.e. P(Y=y) = 2^{-n} (Y is uniform because the BSC is doubly stochastic), so P(X=x|Y=y) = 2^{-n} channel p y x / 2^{-n} = channel p y x and P(f(X)=1|Y=y) = sum_x channel p y x * 1[f x] = outputPosterior f p y. Also `channel_symm`. Fintype.card (BitVector n) = 2^n proved; any Fintype univ is complete (Finset.mem_univ), so the sums range over all 2^n vectors with no duplicates.

- (b) Entropy/junk values: entropy x = -x*Real.log x - (1-x)*Real.log(1-x); Real.log is the natural log (checked Real.log (Real.exp x) = x, Real.exp (Real.log 2) = 2). Real.log 0 = 0 makes 0*ln 0 = 0, exactly the manuscript's convention '0 ln 0 := 0'; entropy 0 = entropy 1 = 0. The other junk branch (Real.log (-x) = Real.log x) is never reached inside the quantifier range: proved `posterior_mem_unit` (0 <= p <= 1 -> outputPosterior in [0,1]) and `mean_mem_unit` (outputMean in [0,1]), and p itself is in [0,1/2]. Division is only by (2:R)^n > 0 (n = 0 gives 1). No convention weakens the statement; on [0,1] Lean's `entropy` coincides with the standard h.

- (c) Quantifier ranges: `#print` with pp.numericTypes shows Target = forall (n : N) (f : BitVector n -> Bool) (p : R), (0:R) <= p -> p <= (1/2 : R) -> retainedInformation f p <= Real.log (2:R) - entropy p. The 1/2 is REAL one-half (not N-division = 0): scratch instantiates the hypothesis at p = 1/3, 0.49, 1/2, 0 by norm_num. n ranges over all N including 0 (instantiated at n = 0; trivially true there since f is constant), a superset of the manuscript's 'every finite n'. f ranges over all 2^(2^n) Boolean functions with no side conditions.

- (d) Mutual information: retainedInformation f p = entropy (outputMean f) - average_y entropy (outputPosterior f p y) = h(P(f(X)=1)) - sum_y 2^{-n} h(P(f(X)=1|Y=y)) = H(f(X)) - H(f(X)|Y) = I(f(X);Y), with the uniform 2^{-n} weight on y justified by (a). Matches manuscript eq. (eq:information) exactly.

- (e) RHS is Real.log 2 - entropy p = ln 2 - h(p) in nats, exactly the manuscript Theorem thm:main in 01_introduction.tex ('All logarithms are natural'; 'For every finite n, every Boolean f, and every p in [0,1/2], I(f(X);Y) <= ln2 - h(p)'). The usual bits form 1 - H(p) is the same inequality divided by ln 2. `CKF2.Spec.matches_f1_target : Target = CK.CourtadeKumar` is by rfl (definitional identity; CK.information/binaryEntropy/posterior/kernel/avg/indicator are letter-for-letter the same as the CKF2.Spec definitions).

- Spot checks (all proved in /private/tmp/claude-501/-Users-dimiter/bbf75097-b8aa-4c7d-a9eb-d3f829b6d13e/scratchpad/SemanticsCheck.lean, lake env lean exit 0, 0 errors, 0 sorry): n = 1, f = coordinate: `coord1_info (p : R) : retainedInformation coord1 p = Real.log 2 - entropy p` (equality for every real p, so the bound is attained -- Target is tight, not a trivially weak statement). n = 2, f = parity: `parity2_posterior : outputPosterior parity2 p y = if xor (y 0) (y 1) then (1-p)^2 + p^2 else 2*p*(1-p)` and `parity2_mean : outputMean parity2 = 1/2`, as expected.

- Independently re-ran: #print axioms CK.courtadeKumar_proved = [propext, Classical.choice, Quot.sound]; #print axioms CKF2.Spec.matches_f1_target likewise; gate log shows exactTargetAccepted := id CK.courtadeKumar_proved.

- Non-gap observations: the header comments in CK/Definitions.lean ('UNCOMPILED SOURCE DRAFT', 'NOT accepted by a Lean kernel') and the doc-comment on CK.posterior ('Establishing that semantic fact is an outstanding task') are stale prose, not semantics; the Bayes-posterior fact is a meta-level justification (now proved in the scratch file) and is not needed inside Lean for Target to mean what it says. No gap between CKF2.Spec.Target and the informal Courtade-Kumar inequality was found.


## Lens 2 — Trust base and tampering

**Verdict: SOUND**

Trust base and tampering audit of the EXACT_CK_TARGET_ACCEPTED_BY_LEAN_AXIOMS_AUDITED result: no tampering found and no way identified for a false theorem to pass. All trusted-input hashes match; the only historical change to a trusted input is a session-5 re-pin of CK/Definitions.lean whose sole diff (reconstructed and hash-verified against the original f0bcb63d… pin) is `import Mathlib` -> 8 specific imports. verify_kernel.py is byte-identical across all copies and fail-closed; lakefile/manifest/toolchain unchanged since the Sep 16 original; mathlib at the pinned rev = upstream v4.19.0 tag, clean tree, oleans untouched since cache download; lean is the elan-managed 4.19.0 whose commit matches the upstream tag; no env overrides, no LEAN_PATH shadowing. Keyword scan of all CK/CKF2 sources found no axiom/sorry/admit/native_decide/instance/attribute/macro/unsafe/opaque/extern etc.; only `set_option autoImplicit false` in Spec.lean and the Bool function `decide` in FourierCap.lean. The real weakness is in the gate's design, not this result: its `lake build` step was a no-op in every run (0.55 s), so it trusted pre-existing oleans via Lake's trace files, and its `manuscript_changed: false` / `independent_kernel_replay: NOT_RUN` fields are hard-coded constants. I closed the olean gap independently: all 42 project oleans are byte-identical to fresh recompiles from source, the gate file recompiles to the exact recorded proof-artifact hash, the 611-declaration extended axiom audit reproduces exactly, and a fresh lean4checker v4.19.0 kernel replay of all 43 modules passes. Remaining trust: the downloaded mathlib olean cache (not replayed with --fresh) and the elan toolchain download; the one bookkeeping discrepancy (ExtendedAxiomAudit.lean edited one minute after the run) does not touch the proof.

### Findings

- (1) TRUSTED INPUT HASHES: all 5 entries of /Users/dimiter/ck_f2/TRUSTED_INPUT_HASHES.json match the live files (lean/CK/Definitions.lean fd9f1dd9…, lean/CKF2/Spec.lean 3ec07e6b…, CompletionGate.lean.template d13bb57a…, DECLARATIONS.json 731cf74b…, lean/lean-toolchain 55e97be9…). CAVEAT: the Definitions.lean pin was RE-PINNED in session 5. The original F2 package (Sep 16, inside ~/Downloads/CK_Lean_Session5_Deliverables.zip: CK_Kernel_Execution_F2/TRUSTED_INPUT_HASHES.json, SHA256SUMS.json, F1_SOURCE_HASHES.json) pinned Definitions.lean at f0bcb63d…. I reconstructed the original by reverse-applying deliverables/SOURCE_DIFF_Definitions.patch; it hashes to exactly f0bcb63d…, and the ONLY difference is the first line `import Mathlib` replaced by 8 specific Mathlib imports. No definition, statement, or lemma text changed. The other 4 pins (Spec.lean, template, DECLARATIONS.json, lean-toolchain) are byte-identical to the Sep 16 originals. So the trust anchor moved once, benignly.

- (2) GATE SCRIPT: /Users/dimiter/ck_f2/verify_kernel.py is byte-identical (sha256 fdc809e9…) to /Users/dimiter/ck_lean/verify_kernel.py, ~/Downloads/CK_Lean_Session8_Deliverables/verify_kernel.py, and the Sep 16 original in the Session5/6 zips. Structure is fail-closed: hash check -> comment-stripped hygiene regex -> `lake env lean --version` pinned to 4.19.0 -> `lake build CK CKF2` -> generated F2AxiomAudit.lean with whitelist parse -> full mode writes the template to F2CompletionGate.lean and runs `lake env lean -o <olean> F2CompletionGate.lean`, parsing `#print axioms CKF2.exactTargetAccepted` against ALLOW={propext, Classical.choice, Quot.sound}; any error/warning text or nonzero exit aborts. A pre-built/stray F2CompletionGate.olean CANNOT fake success: the gate recompiles the template from source and overwrites the olean; success requires lean rc=0 plus parsed axiom output. Timestamps are consistent: F2CompletionGate.olean 20:55:19 (after CK.olean 20:55:11, gate dir 20:55); its sha256 ea9e5112… equals RESULT.json proof_artifact_sha256, and my own recompile of F2CompletionGate.lean produced a byte-identical olean printing `theorem CKF2.exactTargetAccepted : CKF2.Spec.Target := id CK.courtadeKumar_proved` with exactly the three axioms.

- (2, WEAKNESS) The gate's `lake build CK CKF2` was a NO-OP in all six recorded runs (gate_out, gate_out2, gate_out3, gate_out_final, gate_out_s8a, gate_out_full: 0.53–0.66 s, build.log = only 'Build completed successfully.'). Lake 5.0's default (BuildConfig.showProgress = verbosity != quiet; non-ANSI when piped) prints '✔ Built X' for every job that actually runs, so nothing was compiled by the gate itself. The gate therefore relied on Lake's .trace/.olean.hash bookkeeping for oleans built earlier (18:21–20:55) by manual builds; a tampered olean with intact trace files would not have been detected by the gate. CLOSED INDEPENDENTLY: I recompiled every one of the 42 project modules from source with `lake env lean -o` into the scratchpad and byte-compared: all 42 oleans (39 CK/*.olean, CK.olean, CKF2.olean, CKF2/Spec.olean) are IDENTICAL to /Users/dimiter/ck_lean/.lake/build/lib/lean/*. Since oleans are deterministic and each compile only reads its imports' oleans, induction over the import DAG shows every project olean is the honest output of its source under the pinned toolchain. All 42 .trace files record the same command shape (`…/leanprover--lean4---v4.19.0/bin/lean FILE -R . -o … -i … -c … --json`), no `-D` options (in particular no debug.skipKernelTC), and .lake/lakefile.olean.trace has options {} and leanHash 6caaee842e94…

- (2, WEAKNESS) Two RESULT.json fields are hard-coded constants in verify_kernel.py line 119, never computed: `manuscript_changed: false` (the gate never reads any .tex file; no manuscript hash is recorded anywhere in the deliverables) and `independent_kernel_replay: NOT_RUN`. Do not read `manuscript_changed: false` as evidence of anything. Minor: parse_axioms' regex `'([^']+)'` cannot parse primed declaration names (e.g. CK.anchorD'); this fails closed (mismatch error), and none of the 32 DECLARATIONS.json names or the gate target are primed. Minor: lakefile.lean is not in TRUSTED_INPUT_HASHES.json, so a `moreLeanArgs := #["-Ddebug.skipKernelTC=true"]` would not be caught by the hash check or the hygiene scan; the live lakefile is clean (no leanOptions/moreLeanArgs) and identical to the Sep 16 original and the Session8 copy.

- (3) BUILD CONFIG UNCHANGED: /Users/dimiter/ck_lean/lake-manifest.json (sha256 5e29f710…) and lean-toolchain are byte-identical to ~/Downloads/CK_Lean_Session7_Deliverables/{lake-manifest.json,lean-toolchain}, to Session7 gate_run/lake-manifest.json, to the Session8 copies, and to the Sep 16 original package; lakefile.lean (sha256 98c05f22…) identical to Session8 and the original. Manifest pins mathlib rev c44e0c8ee63ca166450922a373c7409c5d26b00b (inputRev v4.19.0). RESULT.json lake_manifest_sha256 matches.

- (4) KEYWORD SCAN of CK.lean, CKF2.lean, CK/*.lean (39 files), CKF2/Spec.lean, comments stripped with the gate's own `uncomment` and cross-checked with a raw grep: ZERO hits for axiom, sorry, admit, native_decide, ofReduceBool, implemented_by, extern, unsafe, opaque, instance, attribute, @[, macro, elab, notation, Decidable, sorryAx, trustCompiler, skipKernelTC. Hits: `set_option autoImplicit false` at CKF2/Spec.lean:11 (benign); `decide` (the Bool-valued function Decidable.decide, not the tactic, not native_decide) at CK/FourierCap.lean:282, 284, 288, 290; the word 'partial' only inside a doc comment at CK/LogEnclosure.lean:8 ('partial sum'); `noncomputable section` in 40 files (benign). Note autoImplicit is NOT disabled in the CK library (Lean default true), but the gate's checked statement CKF2.Spec.Target lives under autoImplicit false with fully explicit binders and is `rfl`-equal to CK.CourtadeKumar (matches_f1_target), so the checked proposition cannot have been altered by an auto-bound implicit.

- (5) MATHLIB: /Users/dimiter/ck_lean/.lake/packages/mathlib is at HEAD c44e0c8ee63ca166450922a373c7409c5d26b00b ('chore: bump toolchain to v4.19.0 (#24519)', 2025-05-01), which equals the manifest pin AND the upstream tag (`git ls-remote origin refs/tags/v4.19.0` -> c44e0c8ee…). `git status --porcelain` is empty (no modified, untracked, or ignored files outside .lake). All 6311 mathlib oleans have mtimes 18:14:34–18:18:27 Sep 17 (the `lake exe cache get` window; ~/.cache/mathlib holds 6643 .ltar files from 18:18); no file in any package's .lake/build is newer than 18:30. Across all 10 LEAN_PATH directories plus the toolchain lib (8160 oleans) there are 0 duplicate module paths and no CK/CKF2/F2 olean outside the project build dir, so no module shadowing during the gate's `lake env lean` steps. RESIDUAL TRUST: mathlib's oleans are the downloaded prebuilt cache, not rebuilt or kernel-replayed here (lean4checker --fresh not run; hours of work); this is standard practice but it is the largest remaining trust assumption.

- (6) LEAN BINARY: `which lean` = /Users/dimiter/.elan/bin/lean (elan proxy) resolving via lean-toolchain to /Users/dimiter/.elan/toolchains/leanprover--lean4---v4.19.0/bin/lean; `lean --version` = 'Lean (version 4.19.0, arm64-apple-darwin23.6.0, commit 6caaee842e94, Release)'; `lake env lean --version` identical; `elan override list` = no overrides; settings.toml default = leanprover/lean4:v4.19.0; no LEAN_*/ELAN_* environment variables set. The commit 6caaee842e94 equals upstream `git ls-remote https://github.com/leanprover/lean4.git refs/tags/v4.19.0` -> 6caaee842e9495688c1567e78c0e68dbb96942aa and the leanHash in .lake/lakefile.olean.trace. Toolchain installed 18:12:28 Sep 17; every file in it (bin/lean, lib/lean/libleanshared.dylib 120 MB, all core oleans) is dated May 1 2025 with nothing newer than Aug 2026, i.e. untouched since install. Residual trust: the elan download itself (GitHub release) was not re-verified against a published checksum.

- (7) SYMLINK + SNAPSHOT: /Users/dimiter/ck_f2/lean -> /Users/dimiter/ck_lean (verified with readlink). RESULT.json source_hashes: 45 of 46 recorded .lean hashes match the current files; the single mismatch is ExtendedAxiomAudit.lean (recorded af783f22…, now f48920c5…, mtime 20:56 = one minute after the 20:55 gate run; the current file equals the Session8 deliverable copy). That file is NOT imported by CK, CKF2, or the gate and has no bearing on the theorem; F2CompletionGate.lean is absent from source_hashes only because the gate writes it after hashing, and it is byte-identical to the template. I re-ran ExtendedAxiomAudit.lean myself (140 s, rc=0, no errors/warnings): 611 `#print axioms` reports, every one a subset of {propext, Classical.choice, Quot.sound}; output byte-identical to ~/Downloads/CK_Lean_Session8_Deliverables/extended_audit/extended_axioms.log. All files in ~/ck_f2/gate_out_full are byte-identical to Session8's gate_full/.

- INDEPENDENT KERNEL REPLAY (beyond the gate): the Session8 note claims a lean4checker replay; the crash report ~/Library/Logs/DiagnosticReports/lean4checker-2026-09-17-205944.ips is a dyld load failure ('__DATA_CONST segment missing SG_READ_ONLY flag'), not a kernel failure, fixed by relinking (sysclang.sh); the deliverable log contains only module headers. I re-ran it myself: the checkout at /private/tmp/claude-501/…/scratchpad/lean4checker is the official leanprover/lean4checker repo at tag v4.19.0 (HEAD e11f65c651ed…, toolchain v4.19.0, clean tree); its Replay.lean adds every constant via Kernel.Environment.addDeclCore and throws on any kernel exception. `lake env lean4checker <module>` returned rc=0 with empty output for all 43 modules: CKF2.Spec, all 39 CK.* modules, CK, CKF2, F2CompletionGate. So every project declaration, including CK.courtadeKumar_proved and CKF2.exactTargetAccepted, was re-accepted by a fresh kernel pass over the oleans (imports, i.e. mathlib, trusted).

- WHAT WAS CHECKED vs WHAT IT MEANS: the accepted proposition is CKF2.Spec.Target = ∀ n f p, 0 ≤ p → p ≤ 1/2 → retainedInformation f p ≤ Real.log 2 − entropy p, with entropy in nats, X uniform on Bool^n, Y the product BSC(p), retainedInformation = H(E[f]) − E_y H(P(f=1|y)) computed by finite sums. The gate forces `change CK.CourtadeKumar` and `matches_f1_target : Target = CK.CourtadeKumar := rfl`, so the definitional identification is kernel-checked. Whether Spec.Target is a faithful transcription of the manuscript's inequality (I(f(X);Y) ≤ ln2 − h(p)) is a statement-fidelity question outside this lens; nothing in the trust base was found that would let a false theorem pass.


## Lens 3 — Proof architecture and fidelity to the manuscript

**Verdict: SOUND**

The kernel-accepted theorem CKF2.exactTargetAccepted states exactly the Courtade-Kumar inequality: for every n, every Boolean f on {0,1}^n and every 0<=p<=1/2, H(f(X)) - H(f(X)|Y) <= ln2 - h(p) in nats, with the posterior and conditional entropy spelled out as finite sums over the product BSC kernel (rows sum to 1, symmetric, so Y is uniform and the posterior of X given Y=y is the kernel row). I reconstructed the full dependency graph from CK.courtadeKumar_proved down through Complementary.lean, ScalarMixing.lean, Pinsker/EntropyBounds/ProfileInduction and the complementary-branch files and matched it step by step against Sections 2-6 and the appendices of the v11 manuscript: every manuscript lemma (smallcoordinate, posterioridentity, prop:induction, thm:linear-mixing with its three regimes and both transfer rules, lem:product, eq:curvature, lem:contraction, lem:bias, lem:largecoordinate with App B, lem:antipodal, lem:rademacher, lem:balancedcap, prop:cap, lem:energy, lem:comparison, the strict anchor, prop:middle, prop:high) has a Lean counterpart with the same or a stronger statement, and in several places (contraction lemma, energy bound at the endpoints, product centering at nu+t=1) Lean is more rigorous than the prose. Each of the newly introduced definitions (chi, fourierCoeff, weight, signOf, noiseOp, signSum, ent, noisy, deficit, infoP, lift, lambdaBar, cInt, plus psi, lambdaDenom, pairEntropy) is the intended mathematical object; I confirmed non-degeneracy by reading the definitions, by the theorems that pin them to concrete values (weight_zero/one, infoP_indicator, lift_snoc_*, noisy_noisy, kernel_factor_sign), and by a scratch file type-checked with lake env lean that evaluates kernel/noisy/fourierCoeff/weight/deficit on one-bit and uniform cases and reprints the axiom sets ([propext, Classical.choice, Quot.sound]) of the top-level theorems. No lemma is vacuous and no conclusion is weaker than its caller needs. The only items worth recording are informational: stale 'uncompiled draft' comments, an ExtendedAxiomAudit.lean edited after the gate (not a trusted input), and the gate's reliance on cached oleans with no independent kernel replay, none of which bears on the meaning of the proved statement.

### Findings

- TARGET FIDELITY (no defect): CKF2.Spec.Target (~/ck_lean/CKF2/Spec.lean) is literally I(f(X);Y) <= ln2 - h(p) for uniform X on {0,1}^n, Y = BSC_p(X), all n, all Boolean f, all 0<=p<=1/2, in nats. channel p y x = prod_i [y_i=x_i ? 1-p : p] is the BSC transition matrix; since rows sum to 1 (CK.kernel_row_sum) and the kernel is symmetric, Y is uniform and P(X=x|Y=y) = channel p y x, so outputPosterior f p y = P(f(X)=1|Y=y), average(entropy o outputPosterior) = H(f(X)|Y), entropy(outputMean f) = H(f(X)). Target = CK.CourtadeKumar by rfl (kernel-checked). Bool in place of {-1,+1} is a relabelling (sign true = 1, sign false = -1). No balance/dimension/Fourier hypothesis in the target.

- AXIOMS (re-verified): my scratch file /private/tmp/claude-501/-Users-dimiter/bbf75097-b8aa-4c7d-a9eb-d3f829b6d13e/scratchpad/Probe.lean run with `lake env lean` prints [propext, Classical.choice, Quot.sound] for CK.courtadeKumar_proved, complementary_proved, lowNoise_proved, linear_scalar_mixing, product_centering, deficit_noisy_le, bias_inclusive_cap, entropy_comparison, strict_anchor, logEnclosure_proved. Static scan of CK/ and CKF2/: zero occurrences of sorry/axiom/admit/native_decide/ofReduceBool/unsafe/implemented_by/extern/opaque; no instance, attribute, macro, or notation declarations; only set_option is `autoImplicit false` in Spec.lean. All numeric log/entropy brackets (CK/Constants.lean) are instantiations of logEnclosure_proved (atanh series with explicit geometric tail, N=12) closed by norm_num on rationals; the only Mathlib numeric fact used is Real.log_two_gt_d9 (in Pinsker.lean).

- DEFINITION AUDIT (all are the intended objects, none degenerate): chi S x = prod_{i in S} sign(x i) (Walsh character; chi_orthonormal, sum_chi_mul_chi proved); fourierCoeff F S = avg(F * chi S); weight F k = sum_{|S|=k} F̂(S)^2 (weight_zero = (avg F)^2, weight_one = sum_i F̂({i})^2; probe: weight(signOf x_0) 1 = 1 on Cube 1); signOf f = sign o f; noiseOp p F y = sum_x kernel p y x * F x = T_rho F with rho = 1-2p (noiseOp_chi: T_rho chi_S = rho^|S| chi_S; posterior_eq_noiseOp); signSum a x = sum_i a_i sign(x_i) (Rademacher sum, moments up to order 10 proved by snoc-induction); ent q = sum negMulLog(q a) (Shannon entropy in nats; probe: deficit of the uniform distribution on Cube k is 0); noisy p q w = sum_u kernel p w u * q u (BSC push-forward; probe on Cube 1: noisy p (delta_true)(false) = p, so it depends on p; noisy_noisy gives the BSC composition law p0+p'-2p0p'); deficit q = k ln2 - ent q; infoP pi = h(avg pi) - avg(h o pi) = I(Z;U) for uniform U (infoP_indicator: = h(mean f) for a deterministic bit, so not identically 0; infoP_posterior: = information f p); lift f (snoc w z) = decide(f(zx) = z), i.e. F♯(x,z) = z F(zx) (lift_snoc_true: = f w; lift_snoc_false: = !f(negCube w); avg_signOf_lift = 0, fourierCoeff_lift_last = m, fourierCoeff_lift_castSucc i = b_i, weight_one_lift = m^2 + W_1); lambdaBar rho = (ln(2/(1-rho)) + 3/4 + rho/4)/(2 Lambda_rho) with lambdaDenom rho = 1+rho-(31/40)rho^2 = eq:lambdabar; cInt t = 2 atanhLog(19t/20) = ln((1+19t/20)/(1-19t/20)) = c(t) of app:interior; also psi rho t = (1-t)(1+t-rho^2), pairEntropy nu z = (eta(nu+z)+eta(nu-z))/2 = Q(nu,z), eta u = h((1+u)/2), kappa = ln2 - eta, atanhLog = atanh, curvature = 1/(1-u^2). kernel on Cube 1 probes: kernel p (true)(false) = p, kernel p (true)(true) = 1-p.

- DEPENDENCY GRAPH vs MANUSCRIPT (every step has a same-or-stronger Lean counterpart): courtadeKumar_proved = assemble_conditional lowNoise_proved complementary_proved (split at p<=1/20; p=0 handled inside the low branch via h(0)=0, p=1/2 inside the high branch via rho=0). LOW BRANCH: lowNoise_proved = lowNoiseCK_of_scalar scalar_mixing -> lowNoiseCK_of_scalar_and_pinsker -> lowNoiseCK_of_profile (Cor cor:low: Algebra.bias_payment with pinsker h(mu)<=ln2-(2mu-1)^2/2 and h(p)<=1/2 [manuscript uses h(p)<1/5; 1/2 suffices]) applied to lowNoiseProfile_of_scalar (Prop prop:induction with c=h(p)) = abstract_profile_induction with base cases profile_zero/profile_one and step profile_step_of_scalar (exists_small_gap = Lemma lem:smallcoordinate proved for the last two coordinates via gap_sum_le_one, which is exactly what the manuscript's proof uses; conditionalEntropy_snoc/posterior_snoc = Lemma lem:posterioridentity; d<=E|A-B| via abs_avg_le_avg_abs = eq:parent). scalar_mixing (p^2/25) <- linear_scalar_mixing (Thm thm:linear-mixing, p/500 kept) = t=0 (scalarSlack_nonneg_of_eq), t=1 (scalarSlack_endpoints, p/4), interior (interior_product = eq:interiorproduct via gain_integral = eq:gainintegral, table_bound = the 5-row table, psi_mono = worst case p=1/20; transfer_product = eq:transferproduct with M<=ln2), endpoint-small (gain_dominance K0>=M0/3, transfer_gain with D=1/2, endpoint_interval 23/3600 = app:rsmall), endpoint-large (endpoint_normalized = app:rlarge+app:normalized, transfer_relative = eq:relative-transfer with pairEntropy_le_eta for M<=M0). transfer_* rest on product_centering : ProductCenteringGoal (Lemma lem:product, stated for all 0<p<1/2, 0<=nu, 0<=t, nu+t<=1; boundary nu+t=1 handled by continuity of pairEntropy on the closed interval rather than a limiting argument) which rests on curvature_identity : CurvatureGoal (eq:curvature). HIGH BRANCH complementary_proved: |m|>=13/20 -> large_bias (Lemma lem:bias: information_le_contraction I<=rho^2 H(f), H(f)<=1/2, kappa_ge_half_sq); exists |b_i|>=13/20 -> large_coordinate (Lemma lem:largecoordinate: swap i to last, information_relabel, large_coordinate_last = conditionalEntropy_ge_conditional [eq:conditionalcontraction, proved by a direct posterior computation with infoP_noisy_le rather than the Markov-chain argument] + sigma_ge [sigma>=b] + U_le_U_zero [App B.1 concavity, D0 decomposition eq:D0decomp verbatim] + large_coord_scalar [eq:largecoordscalar]; the b<0 case by output complement information_compl); else bias_inclusive_cap (Prop prop:cap via lift and balanced_first_level = Lemma lem:balancedcap: case A weight_one_le_of_small_coeffs via capped_sign_sum = Lemma lem:rademacher with Algebra.majorant = eq:majorant and the exact moments; case B weight_one_le_of_large_last via weight_one_indicator_le = Lemma lem:antipodal and Algebra.section_bound = eq:sectioncap) then middle_range (Prop prop:middle: entropy_comparison = eq:comparison [envelope_of_criterion = eq:lambdacriterion, lambdaReq_le_lambdaBar, lambdaBar_convexOn, 4 knots, cubic0/1/2 Bernstein certificates], energy_lower_bound = Lemma lem:energy [cancellation = eq:cancellation from Plancherel, cancel_sum_le, FG<=|G|], middle_core = eq:biaspayment with Gamma/Lambda<=1240/1321 and eta(3/5)<13/25) or high_noise (Prop prop:high: strict_anchor = eq:anchorcomparison, information_anchor_le I(f;Y_{1/5})<=9/50 [comb_log2], information_degrade = Lemma lem:contraction as degradation via kernel_compose/noisy_noisy, then rho^2/2<=kappa rho). Lemma lem:contraction itself = infoP_noisy_le <- infoP_eq_deficit (I(Z;U) = mu deficit(q1)+(1-mu) deficit(q0)) + deficit_noisy_le (induction on dimension: deficit_chain, onebit_step = kappa_scaling = eq:entropyfacts from the series hasSum_kappa = eq:series, deficit_mix_le = concavity); this is more rigorous than the manuscript's sketch.

- PLACES WHERE LEAN IS STRONGER OR TAKES A DIFFERENT (VALID) ROUTE, not gaps: (a) energy_lower_bound is proved outright for 0<=rho<=1 (no 'extend by continuity'); (b) large_coord_scalar_endpoint proves G_rho(13/20)>=0 by bounding the everywhere-nonnegative series (1-c rho^2)kappa(rho)-(1-rho^2)kappa(c rho) below by its first six terms (a degree-14 polynomial inequality on [0,9/10] closed by nlinarith) instead of App B.2's concavity-in-x chord argument; the extension to b in [13/20,1] uses concaveOn_eta exactly as in the manuscript; (c) endpoint_normalized works with Psi = (a*-kappa)(a*+r beta) - z*^2 >= 0, which is algebraically equivalent to Phi = a* - z*^2/B* >= kappa, and redoes the r-monotonicity for Psi (gfun_deriv_nonneg) plus the r=e^v boundary via gfun_exp_nonneg using Algebra.endpoint_polynomial_identity/margin and B*<=8; (d) the low-noise branch consumes only the weaker ScalarMixingClaim (p^2/25) although the p/500 statement is proved; (e) abstract_profile_induction's hypothesis d<=1/2 is unused by the algebra (the scalar lemma is the consumer), harmless.

- NOT FORMALIZED (not needed for thm:main, so not a gap): the generic certificate Prop prop:certificate with free Omega, lambda, zeta (only its two instances middle_core/high_core exist), Remark rem:deficit's four-term identity (Algebra.deficit_identity exists but is unused), and all of Section 7 (stability).

- MINOR INTEGRITY NOTES (outside proof-fidelity, informational): ~/ck_f2/lean is a symlink to ~/ck_lean; 45 of 46 source hashes in gate_out_full/RESULT.json match the bytes on disk that I read; the one mismatch is ExtendedAxiomAudit.lean (mtime 20:56:04, after the gate finished at 20:55:19), which is not a trusted input, not in DECLARATIONS.json, and not imported by the gate. All five TRUSTED_INPUT_HASHES.json entries match. All CK/*.lean edits predate the oleans (built 20:55:10-11) which predate the gate (20:55:12-19); the gate's `lake build CK CKF2` took 0.55 s (cache hit), and RESULT.json records independent_kernel_replay = NOT_RUN, so the trust base is the .olean files on disk plus Lean 4.19.0 / Mathlib v4.19.0; my own lake env lean run reproduced the axiom output from the same oleans.

- COSMETIC: stale headers still say 'UNCOMPILED SOURCE DRAFT', 'NOT COMPILED', 'THIS GATE IS EXPECTED TO FAIL', 'NOT YET ELABORATED' in CK/Definitions.lean, CK/Algebra.lean, CK/AnalyticTargets.lean, CK/Induction.lean, CK/Semantics.lean, Audit.lean, verify_lean.sh and the CompletionGate template; they contradict the current state and could mislead a reader, but have no logical effect.


## Lens 4 — Completeness critic

**Verdict: SOUND**

The claim 'Lean 4.19.0 + Mathlib v4.19.0 accept CKF2.exactTargetAccepted : CKF2.Spec.Target with axioms [propext, Classical.choice, Quot.sound]' is true and I re-verified it beyond the gate: the requested lean4checker (official v4.19.0 tag, verified against upstream, with negative controls failing as they should) replays CK.Complementary (3 theorems incl. CK.courtadeKumar_proved) with exit 0 and no output, and my own replay of all 43 project modules (942 constants) also passes 43/43 in 177 s; two modules recompiled from source are byte-identical to the on-disk oleans; the proof-artifact hash in RESULT.json matches the olean on disk; the trusted-input hashes match and the one historical re-pin is a documented imports-only change. Reading the ~50 lines of Spec.lean/Definitions.lean myself and re-running reviewer 1's scratch file (0 errors; equality with the bound at n = 1 proved, so the target is tight and not vacuous), the formal statement is the Courtade-Kumar inequality in nats, uniform X on Bool^n, product BSC(p), all n, all Boolean f, all real 0 ≤ p ≤ 1/2, exactly the manuscript's thm:main. No reviewer finding is a defect in the theorem; the real (minor) defects are in the gate tooling: hard-coded manuscript_changed/independent_kernel_replay fields, a no-op `lake build` step, lakefile not pinned, stale 'uncompiled draft' comments, no git commit. What remains trusted is the standard Lean/Mathlib base plus single-machine provenance: the Lean 4.19.0 kernel (lean4checker shares it, so no independent checker has run), Mathlib's prebuilt olean cache (3275 modules / ~637k constants, downloaded without cryptographic verification, not rebuilt or --fresh replayed - the largest residual), the elan toolchain download, one laptop/one account/one AI-session lineage for every check, and the human reading of the definitions. Epistemic status: proved in Lean to the normal standard of a Mathlib-based formalization, strengthened by a same-toolchain kernel replay of every project declaration; the residual risk is concentrated in tampered-or-corrupt Mathlib oleans or a kernel bug (both remote) and in the absence of any independent-machine reproduction, which is what a referee should ask for next (CI on a fresh runner, one --fresh replay, human sign-off on the statement).

### Findings

- REQUESTED lean4checker RUN: the binary exists (/private/tmp/claude-501/-Users-dimiter/bbf75097-b8aa-4c7d-a9eb-d3f829b6d13e/scratchpad/lean4checker/.lake/build/bin/lean4checker, Mach-O arm64, 119.5 MB, built Sep 17 21:00) from a clean checkout of the official leanprover/lean4checker repo at HEAD e11f65c651edd58d68ba260015d2bfde5102cd7f, which I confirmed equals the upstream refs/tags/v4.19.0 via git ls-remote; lean-toolchain v4.19.0. `cd ~/ck_lean && lake env <binary> CK.Complementary` exited 0 with empty stdout/stderr in 3.16 s wall; `--verbose` prints only `replaying CK.Complementary`. That module holds exactly 3 constants (all theorems: CK.large_coordinate, CK.complementary_proved, CK.courtadeKumar_proved; counted with env.getModuleIdxFor?), so the top theorem's proof term was re-sent to the kernel (Kernel.Environment.addDeclCore, throwing on any kernel exception per Lean4Checker/Replay.lean) against an environment loaded from the on-disk oleans.
- NEGATIVE CONTROL (the silent pass is meaningful): the same binary on its own test oleans fails exactly as test.sh expects: Lean4CheckerTests.AddFalse -> exit 1 '(kernel) declaration type mismatch, false has type Prop but it is expected to have type False'; ReplaceAxiom -> exit 1 '(kernel) application type mismatch False.elim @propext ...'; AddFalseConstructor -> exit 1 'No such constructor False.intro'.
- FULL PROJECT REPLAY (my own, reproducing reviewer 2): all 43 modules (39 CK.*, CK, CKF2.Spec, CKF2, F2CompletionGate) replayed one at a time: 43/43 exit 0 with no output, 177 s total. These modules contain 942 constants in total, so every project declaration including CKF2.exactTargetAccepted has been re-accepted by the v4.19.0 kernel independently of the elaborator. Scope limit: this is the non-fresh mode; the import closure (3275 modules, 637,589 constants for `import CK` + `CKF2.Spec`) was loaded from oleans and NOT re-checked. I did not launch `lean4checker --fresh F2CompletionGate` (multi-hour, multi-GB job on this 24 GB / 10-core laptop); that remains the outstanding step.
- RESULT.json (~/ck_f2/gate_out_full): status EXACT_CK_TARGET_ACCEPTED_BY_LEAN_AXIOMS_AUDITED; independent_kernel_replay = "NOT_RUN" and manuscript_changed = false are both literal constants in verify_kernel.py's initial result dict and are never updated (the script has no replay step and never reads any .tex), so the first is honest and the second carries no information; proof_artifact_sha256 = ea9e5112eee2070d7cf35881058024aa2eba26a2bc5efc1da15c05ef63a27253 equals sha256 of ~/ck_lean/.lake/build/lib/lean/F2CompletionGate.olean on disk (mtime 20:55:19); full_ck_gate.log = `theorem CKF2.exactTargetAccepted : CKF2.Spec.Target := id CK.courtadeKumar_proved` with axioms [propext, Classical.choice, Quot.sound]; build.json shows `lake build CK CKF2` rc=0 in 0.554 s with log 'Build completed successfully.' (cache no-op confirmed); F2CompletionGate.lean is byte-identical to CompletionGate.lean.template; all 5 TRUSTED_INPUT_HASHES entries re-verified; ~/ck_f2/lean -> /Users/dimiter/ck_lean; 45/46 source_hashes match, the sole mismatch being ExtendedAxiomAudit.lean (mtime 20:56:04, after the gate; not imported by the gate).
- STATEMENT (read independently): CKF2/Spec.lean defines entropy x = -x*Real.log x - (1-x)*Real.log(1-x), average g = (Σ_x g x)/2^n over Fin n -> Bool, channel p y x = Π_i (if y i = x i then 1-p else p), outputPosterior = Σ_x channel·bitValue(f x), retainedInformation = entropy(outputMean f) - average(entropy ∘ outputPosterior), Target = ∀ n f p, 0 ≤ p -> p ≤ 1/2 -> retainedInformation f p ≤ Real.log 2 - entropy p; matches_f1_target : Target = CK.CourtadeKumar is rfl. This is I(f(X);Y) ≤ ln 2 - h(p) in nats for uniform X on Bool^n through the product BSC(p), exactly manuscript thm:main (01_introduction.tex lines 4-19: natural logs, 0 ln 0 := 0, I = h(μ) - H(f(X)|Y)), with Bool a relabelling of {-1,1} and n = 0 included (superset). I re-ran reviewer 1's SemanticsCheck.lean myself: exit 0, zero errors/warnings; pp.numericTypes prints the bound as (1/2 : ℝ); channel_row_sum (rows sum to 1 for all real p) justifies the Bayes-posterior reading; coord1_info proves retainedInformation coord1 p = Real.log 2 - entropy p for every p, so the target is attained at n = 1 and is not vacuous or trivially weak.
- OLEAN PROVENANCE SPOT CHECK: recompiling CK/Complementary.lean and CKF2/Spec.lean from source with `lake env lean -o` into the scratchpad gives oleans byte-identical to the on-disk ones (sha256 fbd8faec... and 66f91806...), consistent with reviewer 2's 42/42 claim; together with the kernel replay this closes the gate's no-op-build weakness for this result.
- RE-PIN OF A TRUSTED INPUT (verified, benign, documented): the Session-5 zip contains the original CK_Kernel_Execution_F2/TRUSTED_INPUT_HASHES.json pinning Definitions.lean at f0bcb63d..., the re-pin file deliverables/gate_run/TRUSTED_INPUT_HASHES_repinned.json (= current fd9f1dd9...), and deliverables/SOURCE_DIFF_Definitions.patch; scratchpad/Definitions.orig.lean hashes to f0bcb63d... and differs from the live file only by `import Mathlib` -> 8 specific Mathlib imports. Caveat for a referee: a hash pin the same party can update is not an independent trust anchor; the trust rests on reading the ~50 lines of Spec.lean + Definitions.lean, which four reviewers have now done.
- ARE ANY REVIEWER FINDINGS REAL DEFECTS? None touches the theorem. Minor real defects in the gate tooling: (a) RESULT.json reports manuscript_changed and independent_kernel_replay as hard-coded constants, so the file over-states what it measured; (b) the gate's `lake build` never compiled anything (0.554 s), so the gate's own evidence is 'pre-existing oleans + Lake trace bookkeeping' rather than a fresh compile (closed independently, above); (c) lakefile.lean is not a trusted input (currently clean, no leanOptions/moreLeanArgs); (d) stale 'UNCOMPILED SOURCE DRAFT' / 'THIS GATE IS EXPECTED TO FAIL' comments in CK/Definitions.lean and the template; (e) ~/ck_lean is not a git repository, so no commit hash pins the sources (only sha256 lists in RESULT.json and deliverable zips). Reviewer 3's note that abstract_profile_induction has an unused hypothesis is harmless.
- WHAT REMAINS TRUSTED / UNVERIFIED: (1) the Lean 4.19.0 C++ kernel, olean deserializer and CollectAxioms (#print axioms); lean4checker links the same kernel, so no independent implementation (nanoda, lean4lean, lean4export + external checker) has checked anything; (2) Mathlib's prebuilt oleans: 3275 modules / ~637k constants in the closure, obtained by `lake exe cache get` (6643 .ltar files in ~/.cache/mathlib); Mathlib's Cache tool names files by Lake source hashes (hash.asLTar) and performs no cryptographic verification of the downloaded content, and .olean.hash files are 64-bit non-cryptographic Lake hashes made by whoever built them; the source tree is clean at the manifest rev = upstream v4.19.0 tag, but that says nothing about olean bytes; not rebuilt from source and not replayed with --fresh - this is the largest residual assumption; (3) the elan toolchain download (no published-checksum verification recorded; lean --version commit 6caaee842e94 matches the upstream tag); (4) one machine (Darwin 25.5.0 arm64), one account, one AI-session lineage using the same binaries for build, gate, reviews and replay - no independent-machine or CI reproduction; (5) the meaning of ~50 lines of definitions plus Mathlib's Real, Real.log, Finset.sum/prod and the Pi Fintype instance - a human judgement, with the Bayes identification of outputPosterior proved only in a scratch file, not in the library; (6) manuscript-to-Lean route correspondence (reviewer 3) is not needed for the theorem's truth and has not been checked here.
- WHAT A SKEPTICAL REFEREE WOULD STILL ASK FOR: (i) a public git repository at a tagged commit with a CI job on a fresh runner that runs `lake exe cache get`, a real `lake build`, the gate, `#print axioms`, and lean4checker over all project modules; (ii) one `lean4checker --fresh F2CompletionGate` run (or a Mathlib rebuild from source, or a lean4export + external-checker pass) to remove the prebuilt-olean assumption; (iii) an independent human expert sign-off on Spec.lean/Definitions.lean, and promotion of channel_row_sum / the posterior-Bayes lemma / the n = 1 tightness example from scratch files into the library so the reading is kernel-backed alongside the theorem; (iv) gate fixes: force recompilation or verify olean hashes, compute or drop the manuscript_changed field, add lakefile.lean to trusted inputs, run lean4checker inside the gate, delete stale comments; (v) archived checksums for the elan and Mathlib-cache downloads.
